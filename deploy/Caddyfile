# Installed to /etc/caddy/Caddyfile by setup-vps.sh.
# No proxy-level auth: the app gates everything behind Strava login
# (session cookie + ALLOWED_ATHLETE_IDS allowlist).
strava.pectum.fr {
	encode gzip

	reverse_proxy 127.0.0.1:3001
}
