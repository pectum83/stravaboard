{
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    // DOM lib for code inside page.evaluate callbacks (runs in the browser)
    "lib": ["ES2023", "DOM"],
    "types": ["node"]
  },
  "include": ["**/*.ts"]
}
